[phases.setup]
nixPkgs = ["python310", "pip"]

[phases.install]
cmds = [
  "pip install -r requirements.txt",
  "cd dashboard/backend && pip install -r requirements.txt"
]

[start]
cmd = "cd dashboard/backend && uvicorn main:app --host 0.0.0.0 --port $PORT --workers 2"
