# AutoCTF — Fully Autonomous Pentest + Patch Agent 🏆

Uses E2B sandbox + official MCPs (Exec, Browserbase, GitHub)  
Finds real vulns → exploits → auto-patches → opens PR with proof

## 2-Minute Demo
```bash
./demo/demo_script.sh